@model BibliotecaBolonMiguel.Models.Domain.Genero
@{
    Layout = "_LayoutDashboard";
}
<div class="container-fluid mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow rounded-4">
                <div class="card-header bg-dark text-white text-center py-3">
                    <h3 class="mb-0"><i class="bi bi-shield-lock"></i>Nuevo Genero</h3>
                </div>
                <div class="card-body p-4 bg-light">
                    <form asp-action="Crear" method="post">
                        <div class="form-group mb-3">
                            <label asp-for="Nombre" class="form-label fw-semibold">Nombre del Genero</label>
                            <input asp-for="Nombre" class="form-control border rounded-3" placeholder="Nombre del genero" required />
                        </div>
                        <div class="form-group mt-4 text-center">
                            <button type="submit" class="btn btn-success w-100 py-2 rounded-3">
                                <i class="bi bi-save me-2"></i> Guardar Rol
                            </button>
                        </div>
                    </form>
                    <div class="text-center mt-3">
                        <a asp-action="Index" class="text-decoration-none text-secondary">
                            <i class="bi bi-arrow-left me-2"></i> Volver al listado
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<style>
    .card {
        background: #ffffff;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .bg-dark {
        background-color: #343a40 !important;
    }
    .form-control {
        padding: 0.75rem;
        font-size: 1rem;
    }
    .btn-success {
        background-color: #28a745;
        border: none;
        transition: background-color 0.3s ease;
    }
    .btn-success:hover {
        background: #218838;
    }
    .text-secondary:hover {
        color: #6c757d !important;
    }
</style>
